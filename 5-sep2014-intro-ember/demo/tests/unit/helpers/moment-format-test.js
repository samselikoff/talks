import {
  momentFormat
} from 'demo/helpers/moment-format';

module('MomentFormatHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = momentFormat(42);
  ok(result);
});
